---
phase: 08-visual-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MainLayout.tsx
  - src/components/StatusBar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "MainLayout header uses only semantic color classes (bg-background, text-foreground, border-border, hover:bg-accent)"
    - "StatusBar connection states use theme-aligned colors instead of hardcoded red/green/blue-900"
    - "No slate-* color classes remain in MainLayout.tsx or StatusBar.tsx"
  artifacts:
    - path: "src/components/MainLayout.tsx"
      provides: "Theme-consistent header and layout"
      contains: "bg-background"
    - path: "src/components/StatusBar.tsx"
      provides: "Theme-consistent status indicators"
  key_links:
    - from: "src/components/MainLayout.tsx"
      to: "index.css :root/.dark variables"
      via: "semantic Tailwind classes"
      pattern: "bg-background|text-foreground|border-border|hover:bg-accent"
---

<objective>
Replace all hardcoded slate-* color classes in MainLayout.tsx and StatusBar.tsx with semantic CSS variable classes to ensure theme consistency.

Purpose: Gaps 1 and 2 from verification — hardcoded colors break theme consistency and won't update if the theme changes.
Output: Both files using only semantic color classes that map to the hue 220 blue-tinted theme defined in index.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-polish/08-VERIFICATION.md
@src/components/MainLayout.tsx
@src/components/StatusBar.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded slate-* colors in MainLayout.tsx</name>
  <files>src/components/MainLayout.tsx</files>
  <action>
    Replace all hardcoded slate-* Tailwind classes with semantic equivalents throughout MainLayout.tsx (lines 166-346):

    Color mapping:
    - bg-slate-900 -> bg-background
    - bg-slate-950 -> bg-background (main area uses same background)
    - bg-slate-800 -> bg-accent (used for interactive hover backgrounds like buttons)
    - text-slate-200 -> text-foreground
    - text-slate-400 -> text-muted-foreground
    - text-slate-500 -> text-muted-foreground
    - text-slate-100 -> text-foreground
    - border-slate-700 -> border-border
    - hover:bg-slate-800 -> hover:bg-accent
    - hover:bg-slate-700 -> hover:bg-accent
    - hover:text-slate-200 -> hover:text-foreground
    - hover:text-slate-100 -> hover:text-foreground
    - hover:bg-blue-500 -> hover:bg-primary (resize handle)
    - active:bg-blue-600 -> active:bg-primary (resize handle)
    - bg-slate-700 -> bg-border (for the resize divider, since it's a visual separator)

    Do NOT change any non-slate/non-hardcoded colors. Do NOT change any component logic.
    Search the ENTIRE file for slate- occurrences, not just lines 166-346.
  </action>
  <verify>
    Run: grep -n "slate-" src/components/MainLayout.tsx
    Expected: zero matches (no remaining slate-* classes)
  </verify>
  <done>MainLayout.tsx contains zero hardcoded slate-* color classes; all replaced with semantic equivalents</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in StatusBar.tsx</name>
  <files>src/components/StatusBar.tsx</files>
  <action>
    Replace hardcoded status colors in StatusBar.tsx (lines 34-39) with theme-aligned alternatives using the hue 220 palette:

    Current -> Replacement:
    - bg-red-900 border-red-800 text-red-100 -> bg-destructive/20 border-destructive/30 text-destructive (error state)
    - bg-green-900 border-green-800 text-green-100 -> bg-emerald-900/50 border-emerald-800/30 text-emerald-200 (connected state — no semantic "success" exists, use muted emerald that blends with hue 220)
    - bg-blue-900 border-blue-800 text-blue-100 -> bg-primary/20 border-primary/30 text-primary (connecting/default state)

    These semi-transparent overlays blend naturally with the hue 220 background rather than introducing pure red/green/blue blocks.
  </action>
  <verify>
    Run: grep -n "bg-red-900\|bg-green-900\|bg-blue-900" src/components/StatusBar.tsx
    Expected: zero matches
  </verify>
  <done>StatusBar uses theme-aligned semi-transparent colors that blend with the hue 220 dark theme</done>
</task>

</tasks>

<verification>
1. `grep -rn "slate-" src/components/MainLayout.tsx` returns no matches
2. `grep -n "bg-red-900\|bg-green-900\|bg-blue-900" src/components/StatusBar.tsx` returns no matches
3. `npm run build` succeeds with no errors
</verification>

<success_criteria>
- MainLayout.tsx and StatusBar.tsx contain zero hardcoded slate-*/red-900/green-900/blue-900 color classes
- All replaced with semantic or theme-aligned alternatives
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-04-SUMMARY.md`
</output>
