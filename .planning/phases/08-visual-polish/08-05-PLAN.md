---
phase: 08-visual-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ChangelistPanel/ChangelistPanel.tsx
  - src/components/ChangelistPanel/ChangelistNode.tsx
  - src/components/ChangelistPanel/ChangelistContextMenu.tsx
  - src/components/ChangelistPanel/ShelvedFilesSection.tsx
  - src/components/FileTree/FileTree.tsx
  - src/components/FileTree/FileNode.tsx
  - src/components/FileTree/FileContextMenu.tsx
  - src/components/shared/FileContextMenuItems.tsx
  - src/components/OutputPanel.tsx
  - src/components/SearchResultsPanel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No transition-colors classes exist in application component files (only allowed in ui/ base components)"
    - "All application components use semantic color classes instead of hardcoded slate-*"
  artifacts:
    - path: "src/components/ChangelistPanel/ChangelistPanel.tsx"
      provides: "Transition-free, semantic-colored changelist panel"
    - path: "src/components/FileTree/FileTree.tsx"
      provides: "Transition-free, semantic-colored file tree"
  key_links:
    - from: "src/components/**/*.tsx (non-ui)"
      to: "index.css semantic variables"
      via: "Tailwind semantic classes"
      pattern: "hover:bg-accent|text-muted-foreground|bg-background"
---

<objective>
Remove all transition-colors classes from application components and replace remaining hardcoded slate-* colors with semantic equivalents across all affected component files.

Purpose: Gaps 3 from verification — transition-colors violates the Phase 08 design decision for instant UI response; remaining slate-* colors in component files break theme consistency.
Output: All application component files use semantic colors and have no transition animations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-polish/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove transition-colors and replace slate-* in ChangelistPanel components</name>
  <files>
    src/components/ChangelistPanel/ChangelistPanel.tsx
    src/components/ChangelistPanel/ChangelistNode.tsx
    src/components/ChangelistPanel/ChangelistContextMenu.tsx
    src/components/ChangelistPanel/ShelvedFilesSection.tsx
  </files>
  <action>
    For all four ChangelistPanel files:

    1. Remove every occurrence of `transition-colors` (delete the class, keep the rest of the className string intact)
    2. Replace hardcoded slate-* colors with semantic equivalents:
       - hover:bg-slate-800 -> hover:bg-accent
       - hover:bg-slate-800/50 -> hover:bg-accent/50
       - hover:bg-slate-800/30 -> hover:bg-accent/30
       - hover:bg-slate-700 -> hover:bg-accent
       - text-slate-400 -> text-muted-foreground (if any)
       - text-slate-200 -> text-foreground (if any)
       - bg-slate-800 -> bg-accent (if any)

    Do NOT modify component logic, only className strings. Do NOT touch ui/ directory files.
  </action>
  <verify>
    Run: grep -rn "transition-colors\|slate-" src/components/ChangelistPanel/
    Expected: zero matches
  </verify>
  <done>All four ChangelistPanel component files have no transition-colors and no hardcoded slate-* colors</done>
</task>

<task type="auto">
  <name>Task 2: Remove transition-colors and replace slate-* in FileTree, shared, and remaining components</name>
  <files>
    src/components/FileTree/FileTree.tsx
    src/components/FileTree/FileNode.tsx
    src/components/FileTree/FileContextMenu.tsx
    src/components/shared/FileContextMenuItems.tsx
    src/components/OutputPanel.tsx
    src/components/SearchResultsPanel.tsx
  </files>
  <action>
    For all six files:

    1. Remove every occurrence of `transition-colors` (delete the class, keep rest of className intact)
    2. Replace hardcoded slate-* colors with semantic equivalents:
       - hover:bg-slate-800 -> hover:bg-accent
       - hover:bg-slate-700 -> hover:bg-accent
       - bg-slate-800 -> bg-accent
       - text-slate-200 -> text-foreground
       - text-slate-400 -> text-muted-foreground
       - hover:bg-slate-750 -> hover:bg-accent (SearchResultsPanel uses this non-standard value)

    Do NOT modify component logic, only className strings. Do NOT touch ui/ directory files.
  </action>
  <verify>
    Run: grep -rn "transition-colors" src/components/ --include="*.tsx" | grep -v "src/components/ui/"
    Expected: zero matches (only ui/ components may have transition-colors)

    Run: grep -rn "slate-" src/components/ --include="*.tsx" | grep -v "src/components/ui/"
    Expected: zero matches
  </verify>
  <done>All application component files are free of transition-colors and hardcoded slate-* colors</done>
</task>

</tasks>

<verification>
1. `grep -rn "transition-colors" src/components/ --include="*.tsx" | grep -v "src/components/ui/"` returns no matches
2. `grep -rn "slate-" src/components/ --include="*.tsx" | grep -v "src/components/ui/"` returns no matches
3. `npm run build` succeeds with no errors
</verification>

<success_criteria>
- Zero transition-colors in application components (ui/ excluded)
- Zero hardcoded slate-* colors in any component file outside ui/
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-05-SUMMARY.md`
</output>
